export interface ServiceItem {
  id: string;
  titleKey: string;
  textKey: string;
  tags: string[];
  icon: string;
}

export interface SkillItem {
  id: string;
  titleKey: string;
  textKey: string;
  class: string;
}
